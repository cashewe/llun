use llun_core::rules::RuleManager;

#[tokio::main]
async fn main() -> Result<()> {
    // use figment to access the 'rules' arg in the users tomls
    // init a rule_manager
    // yield the rules object back to the agent
    Ok(())
}